"""clinic — MediCare Patient Records Manager package.

Modules:
    models      Patient class (encapsulation + factory method)
    validators  pure cleaning/validation functions
    storage     CSV loading and JSON report writing
"""
